name: finova_loan_calculator
description: Finova - Smart Finance Toolkit
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

  # Save language + settings
  shared_preferences: ^2.2.3

  # Number formatting (currency/locale)
  intl: ^0.19.0

  # Charts (pie chart)
  fl_chart: ^0.68.0

  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
